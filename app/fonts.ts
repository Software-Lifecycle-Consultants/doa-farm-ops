//Import fonts
import { DM_Sans, Inter } from 'next/font/google'

// Define 'dm_sans' to store the DM Sans font configuration.
export const dm_sans = DM_Sans({
  subsets: ['latin'],
  display: 'swap',
})

// Define 'inter' to store the Inter font configuration.
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
